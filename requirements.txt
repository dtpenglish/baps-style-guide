# BAPS DTP Style Guide — Python dependencies
# Version: 1.0
# Install with: pip install -r requirements.txt

mkdocs-material>=9.5.0
mkdocs-git-revision-date-localized-plugin>=1.2.4
mkdocs-glightbox>=0.3.7
mike>=2.0.0
# PDF export support.
# - mkdocs-print-site-plugin generates a consolidated /print_page/
#   containing every page in nav order.
# - Playwright drives headless Chromium to print that page to PDF.
# First-time setup on a fresh machine:
#   pip install -r requirements.txt
#   python -m playwright install chromium
# Then build the PDF with:
#   python build-pdf.py
mkdocs-print-site-plugin>=2.8
playwright>=1.40.0
